# coffee-time-app


1. Change DB name and credentials from `config/db.php`

2. Install dependencies with Composer 

    ```
    composer install  
    ```
3. Run migrations

    ```
    php yii migrate  
    ```
